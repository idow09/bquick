name: bquick
description: BQuick 2.0 is coming

version: 0.9.1+1

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^0.2.5
  firebase_analytics: ^1.0.4
  rxdart: ^0.19.0
  shared_preferences: ^0.4.3
  intl: ^0.15.7
  mockito: ^4.0.0
  cupertino_icons: ^0.1.2

dev_dependencies:
  test: ^1.5.1+1
  flutter_launcher_icons: ^0.7.0
  flutter_test:
    sdk: flutter

flutter_icons:
  android: "ic_launcher"
  ios: true
  image_path: "assets/icon/web_hi_res_512.png"

flutter:
  uses-material-design: true